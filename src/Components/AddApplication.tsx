import { type JSX } from 'react'
import './Button.scss'

const AddApplication = (): JSX.Element=> {
  return (
    <div>
        <button>
            Add Application
        </button>       
    </div>
  )
}

export default AddApplication